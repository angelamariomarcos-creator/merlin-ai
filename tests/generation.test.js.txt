// ==========================================================
// tests/generation.test.js
// ==========================================================
const request = require('supertest');
const app = require('../server'); // Importa el servidor unificado expuesto en la raíz

describe('Image Generation API', () => {
    
    test('GET /health returns status', async () => {
        const res = await request(app).get('/health');
        expect(res.statusCode).toBe(200);
        expect(res.body.status).toBe('ok');
    });
 
    test('GET /health shows API availability', async () => {
        const res = await request(app).get('/health');
        expect(res.body.apis).toBeDefined();
    });
});